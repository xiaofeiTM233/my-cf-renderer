{
  "name": "my-cf-renderer",
  "version": "1.0.0",
  "private": true,
  "description": "A Cloudflare Worker for rendering HTML templates to images using Browser Rendering.",
  "scripts": {
    "start": "wrangler dev",
    "deploy": "wrangler deploy",
    "build": "tsc"
  },
  "dependencies": {
    "@cloudflare/puppeteer": "^1.0.4",
    "mustache": "^4.2.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20231025.0",
    "@types/mustache": "^4.2.5",
    "typescript": "^5.2.2",
    "wrangler": "^3.15.0"
  }
}